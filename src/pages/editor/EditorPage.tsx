import React from "react";
import Editor from "../../components/code-editor/Editor";

const EditorPage = () => {
  return (
    <div>
      <Editor problem={false}></Editor>
    </div>
  );
};

export default EditorPage;
